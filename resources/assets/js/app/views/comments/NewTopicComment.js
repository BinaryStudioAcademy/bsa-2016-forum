var Marionette = require('backbone.marionette');

module.exports = Marionette.ItemView.extend({
    template: 'NewTopicComment',

    events: {
        'click': function (event) {

        }
    },

    initialize: function (options) {

    },

});